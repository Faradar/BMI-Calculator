// Create a class for the users
class User{
    constructor(name, height, weight, bmi) {
        this.name = name;
        this.height = height;
        this.weight = weight;
        this.bmi= bmi;
    }
}

// Declare array to store user objects
let users = [];